name: Schedule Workflow

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  run-scripts:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Schedule Workflow Executed"
